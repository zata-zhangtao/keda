# =============================================================================
# Environment Configuration Template
# =============================================================================
# Copy this file to .env and fill in your actual values.
# DO NOT commit .env to version control - it contains secrets!
#
# Configuration Priority (highest to lowest):
# 1. Environment variables
# 2. .env / .env.local files
# 3. config.toml
# 4. Code defaults
# =============================================================================

# =============================================================================
# Application Settings
# =============================================================================
# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
# LOG_LEVEL=INFO

# JWT signing secret (generate with: openssl rand -hex 32)
API_SECRET_KEY=your-256-bit-secret-key-here

# Admin login identifier (default: admin)
ADMIN_IDENTIFIER=admin

# Bcrypt hashed admin password (generate with: python -c "import bcrypt; print(bcrypt.hashpw(b'password', bcrypt.gensalt()).decode())")
# In Dokploy/Compose env files, escape each $ as $$.
ADMIN_PASSWORD_HASH=$$2b$$12$$...

# =============================================================================
# Database Credentials (PostgreSQL)
# =============================================================================
# Complete database URL. Overrides config.toml defaults.
# Format: postgresql+psycopg2://user:password@host:port/database
DATABASE_URL=postgresql+psycopg2://app:app@localhost:5432/app

# =============================================================================
# AI Model API Keys
# =============================================================================
# Uncomment the ones your app uses.

# DashScope (Alibaba Qwen)
# DASHSCOPE_API_KEY=sk-your-dashscope-key

# OpenAI
# OPENAI_API_KEY=sk-your-openai-key

# Anthropic
# ANTHROPIC_API_KEY=sk-ant-your-anthropic-key

# OpenRouter (multi-provider routing)
# OPENROUTER_API_KEY=sk-your-openrouter-key

# Vector Engine
# VECTORENGINE_API_KEY=sk-your-vectorengine-key

# =============================================================================
# Preview Deployment Secrets (GitHub Actions -> deploy-preview.yml)
# =============================================================================
# These values are only required when using the PR preview deployment workflow.
# They should be stored in GitHub Secrets, not in this file.
# The placeholders below document the expected variables.
# =============================================================================
# SERVER_HOST=preview.example.com
# SERVER_USER=deploy
# SERVER_SSH_KEY=-----BEGIN OPENSSH PRIVATE KEY-----
# REGISTRY_USERNAME=your-registry-username
# REGISTRY_PASSWORD=your-registry-password-or-token
# POSTGRES_PASSWORD=change-me-in-secrets

# S3 endpoint URL. Examples:
#   AWS S3:       https://s3.amazonaws.com
#   Backblaze B2: https://s3.us-east-005.backblazeb2.com
#   MinIO:        http://minio:9000
S3_ENDPOINT=
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_BUCKET=app-backups
S3_PREFIX=app-backups
BACKUP_TIME=18:00
FULL_BACKUP_DAY=6
RETENTION_DAYS=30
